# CC小记应用签名配置（示例）
# 将本文件复制为 keystore.properties 并填入真实值。
# 注意：keystore.properties 与 *.keystore 已在 .gitignore 中忽略，请勿提交到版本库。

# 推荐：将密钥库存放在 repo 内的本地忽略目录
# 也可使用绝对路径或仓库外的路径（例如 ../ccxiaoji_release.keystore）
storeFile=tools/secrets/ccxiaoji_release.keystore

# 密钥库密码
storePassword=你的密钥库密码

# 密钥别名
keyAlias=ccxiaoji_release

# 密钥密码
keyPassword=你的密钥密码

# 也可以使用以下环境变量覆盖上述配置（无需提供 keystore.properties）：
#   KEYSTORE_FILE, KEYSTORE_PASSWORD, KEY_ALIAS, KEY_PASSWORD
